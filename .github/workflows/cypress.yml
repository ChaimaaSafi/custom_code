name: Cypress Tests
on: [push]
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Cypress run
        uses: cypress-io/github-action@v3
        with:
          start: npm run dev
          wait-on: 'http://localhost:3000' 
        
        env:
          CYPRESS_RECORD_KEY: "cypress_record_key: npx cypress run --record --key e5d24fd0-5e10-4784-aa1d-1370a16ed9c4"
          SECRET_GITHUB_TOKEN: "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"